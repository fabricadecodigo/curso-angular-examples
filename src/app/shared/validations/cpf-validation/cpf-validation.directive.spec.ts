import { CpfValidationDirective } from './cpf-validation.directive';

describe('CpfValidationDirective', () => {
  it('should create an instance', () => {
    const directive = new CpfValidationDirective();
    expect(directive).toBeTruthy();
  });
});
