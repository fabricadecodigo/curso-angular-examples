import { InvalidNameDirective } from './invalid-name.directive';

describe('InvalidNameDirective', () => {
  it('should create an instance', () => {
    const directive = new InvalidNameDirective();
    expect(directive).toBeTruthy();
  });
});
